@{
    ViewBag.Title = "Create Docker Compose";
}

<h2>Create Docker Compose</h2>

<form asp-action="Create" method="post">
    <!--div class="form-group">
        <label for="filePath">File Path</label>
        <input type="text" id="filePath" name="filePath" class="form-control" />
        <!--span asp-validation-for="FilePath" class="text-danger"></!--span>
    </!--div-->
    <div class="form-group">
        <label for="yamlContent">Docker Compose YAML</label>
        <textarea id="yamlContent" name="yamlContent" class="form-control" rows="10"></textarea>
        @if (ViewData["Error"] != null)
        {
            <div class="alert alert-danger">
                @ViewData["Error"]
            </div>
        }
    </div>
    <button type="submit" class="btn btn-primary">Save</button>
</form>
